% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{kardl_extract}
\alias{kardl_extract}
\title{Extract Components from kardl Objects}
\usage{
kardl_extract(kardl_object, what, variable = NULL, component = NULL)
}
\arguments{
\item{kardl_object}{A supported object produced by the \strong{kardl} package.}

\item{what}{A character string specifying the component to extract. The
available options depend on the class of \code{object} and are documented in the
sections below.}

\item{variable}{An optional character string specifying a particular variable
to extract results for, when applicable. The available options depend on the
selected \code{what} value and are documented in the relevant sections below. This
argument is available just for \code{kardl_symmetric} objects, where users can
extract results for specific variables included in the symmetry test. For
example, if a symmetry test includes variables "ER" and "PPI", users can
specify \code{variable = "ER"} to extract results related to the "ER" variable, or
\code{variable = c("ER", "PPI")} to extract results for both variables.
If \code{variable} is not specified when extracting components that include
variable-specific results, the function will return results for all
variables included in the symmetry test.}

\item{component}{An optional character string specifying a particular
component to extract, when applicable. This argument is available just for
\code{kardl_symmetric} objects, where users can specify whether they want to
extract long-run or short-run results from the symmetry test.
For example, if \code{what} is set to \code{"long_wald_tests"}, users can specify
\code{component = "longrun"} to extract long-run Wald test results, or
\code{component = "H0"} to extract the long-run null hypothesis description.
If \code{component} is not specified when extracting components that include
multiple subcomponents (e.g., both long-run and short-run results),
the function will return all available subcomponents for the selected
\code{what} value.}
}
\value{
The requested component. The returned object depends on the class of \code{object}
and the selected value of \code{what}.
}
\description{
\code{kardl_extract()} is a generic accessor for retrieving selected documented
components from objects produced by the \strong{kardl} package.
}
\details{
It provides a stable user-facing interface for accessing important results
without requiring users to rely on the internal list structure of returned
objects.
}
\section{Supported classes}{

\code{kardl_extract()} currently supports:
\itemize{
\item \code{kardl_lm}
\item \code{kardl_mplier}
\item \code{kardl_boot}
\item \code{kardl_test}
\item \code{kardl_test_summary}
\item \code{kardl_symmetric}
}
}

\section{Components for \code{kardl_lm} objects}{

For fitted \code{kardl_lm} model objects returned by \code{\link[=kardl]{kardl()}}, \code{what}
may be one of:
\itemize{
\item{\strong{data_ts_info}:  time-series information about the input data.}
\item{\strong{data_is_ts}:  whether the input data is a time series.}
\item{\strong{data_class}:  class of the input data.}
\item{\strong{data_start}:  starting time of the input data.}
\item{\strong{data_end}:  ending time of the input data.}
\item{\strong{data_frequency}:  frequency of the input data.}
\item{\strong{data_deltat}:  delta time of the input data.}
\item{\strong{data_tsp}:  time-series properties of the input data.}
\item{\strong{data_time}:  time index of the input data.}
\item{\strong{no_constant}:  Whether the model excludes an intercept.}
\item{\strong{trend}:  Trend specification.}
\item{\strong{asym_long_vars}:  Variables with long-run asymmetry.}
\item{\strong{asym_short_vars}:  Variables with short-run asymmetry.}
\item{\strong{deterministic}:  Deterministic regressors.}
\item{\strong{dependent_var}:  Dependent variable.}
\item{\strong{independent_vars}:  Independent variables.}
\item{\strong{all_vars}:  All variables used in the model.}
\item{\strong{all_asym_vars}:  All asymmetric variables.}
\item{\strong{indep_as_excluded}:  Short-run asymmetric variables excluded from
the linear set.}
\item{\strong{indep_al_excluded}:  Long-run asymmetric variables excluded from
the linear set.}
\item{\strong{short_run_vars}:  Short-run variables.}
\item{\strong{long_run_vars}:  Long-run variables.}
\item{\strong{shortrun_length}:  Number of short-run terms.}
\item{\strong{lag_rows_number}:  Number of rows lost due to lag construction.}
\item{\strong{model_type}:  Model type.}
\item{\strong{data}:  Prepared data used internally.}
\item{\strong{start_time}:  Starting time of the estimation sample.}
\item{\strong{end_time}:  Ending time of the estimation sample.}
\item{\strong{span}:  Time span of the estimation sample.}
\item{\strong{opt_lag}:  Selected optimal lag order.}
\item{\strong{lag_criteria}:  Lag-selection criterion values.}
\item{\strong{all_cr_lags}:  Candidate lag combinations and criterion values.}
\item{\strong{k}:  Number of regressors used in relevant post-estimation
procedures.}
\item{\strong{n}:  Effective sample size.}
}
}

\section{Components for \code{kardl_mplier} objects}{

For dynamic multiplier objects returned by \code{\link[=mplier]{mplier()}}, \code{what} may be one of:
\itemize{
\item{\strong{multipliers}: Estimated dynamic multipliers.}
\item{\strong{omega}:  Adjustment-related multiplier matrix.}
\item{\strong{lambda}:  Short-run coefficient matrix used in multiplier
construction.}
\item{\strong{vars}:  Variables included in the multiplier calculation.}
\item{\strong{horizon}:  Multiplier horizon.}
}
}

\section{Components for \code{kardl_boot} objects}{

For bootstrapped multiplier objects returned by \code{\link[=bootstrap]{bootstrap()}}, \code{what} may be
one of:
\itemize{
\item{\strong{multipliers}:  Bootstrapped dynamic multiplier results.}
\item{\strong{level}:  Confidence level used for bootstrap intervals.}
\item{\strong{replications}:  Number of bootstrap replications.}
\item{\strong{vars}:  Variables included in the bootstrap procedure.}
\item{\strong{horizon}:  Multiplier horizon.}
}
}

\section{Components for \code{kardl_test} objects}{

the \code{kardl_test} class includes objects returned by \code{\link[=pssf]{pssf()}}, \code{\link[=psst]{psst()}},
and \code{\link[=narayan]{narayan()}}

For test objects, \code{what} may be one of:
\itemize{
\item{\strong{type}:  Type of test.}
\item{\strong{case}:  Deterministic case used in the test.}
\item{\strong{statistic}:  Test statistic.}
\item{\strong{method}:  Test method description.}
\item{\strong{alternative}:  Alternative hypothesis.}
\item{\strong{data.name}:  Name of the data or model object.}
\item{\strong{sample.size}:  Effective sample size.}
\item{\strong{hypotheses}:  Textual description of the null and alternative
hypotheses.}
\item{\strong{var_names}:  Variables involved in the test.}
\item{\strong{k}:  Number of regressors entering the bounds test.}
\item{\strong{n}:  Sample size, when available.}
\item{\strong{sig}:  Significance-level information.}
\item{\strong{notes}:  Additional notes.}
}
}

\section{Components for \code{kardl_test_summary} objects }{


the \code{kardl_test_summary} class includes summary objects produced by
\code{summary()} methods for \code{kardl_test} objects, such as those produced
by \code{summary.pssf()}, \code{summary.psst()}, and \code{summary.narayan()}.

For test summary objects, \code{what} may be one of:
\itemize{
\item{\strong{statistic}:  Test statistic.}
\item{\strong{case}:  Textual description of the deterministic case.}
\item{\strong{variables}:  Variables included in the tested restriction.}
\item{\strong{decision}:  Textual test decision.}
\item{\strong{hypotheses}:  Textual description of the null and alternative
hypotheses.}
\item{\strong{numeric_decision}:  Numeric encoding of the test decision.}
\item{\strong{significance_level}:  Significance level used for the decision.}
\item{\strong{critical_values}:  Lower and upper critical-value bounds.}
\item{\strong{k}:  Number of regressors entering the bounds test.}
\item{\strong{notes}:  Additional notes.}
}
}

\section{Components for \code{kardl_symmetric} objects}{

The \code{kardl_symmetric} class includes objects returned by the
\code{\link[=symmetrytest]{symmetrytest()}} function, which tests for long-run and short-run asymmetry
in models fitted with \code{\link[=kardl]{kardl()}} that include asymmetric terms.
For symmetry test objects, \code{what} may be one of:
\itemize{
\item{\strong{long_wald_summary}:  Summary of long-run Wald test results.}
\item{\strong{long_hypotheses}:  Textual description of
long-run null and alternative hypotheses.}
\item{\strong{short_wald_summary}:  Summary of short-run Wald test results.}
\item{\strong{short_hypotheses}:  Textual description of
short-run null and alternative hypotheses.}
\item{\strong{long_wald_tests}:  Detailed long-run Wald test results.}
\item{\strong{short_wald_tests}:  Detailed short-run Wald test results.}
\item{\strong{vars}:  Variables included in the symmetry test.}
\item{\strong{type}:  Type of symmetry test.}
\item{\strong{call}:  Original function call that produced the object.}
}
\code{component} and \code{variable} arguments allow for further subsetting of the
symmetry test results, enabling users to extract specific components
(long-run or short-run) and/or results for specific variables of interest.
}

\examples{
kardl_model <- kardl(DriversKilled ~ asym(PetrolPrice + drivers), data = Seatbelts,
mode = c(2, 1, 0, 4, 0 ))

# Examples of extracting components from a fitted kardl_lm model object
# kardl_extract(kardl_model, what = "data_ts_info")
kardl_extract(kardl_model, what = "data_is_ts")
kardl_extract(kardl_model, what = "data_class")
kardl_extract(kardl_model, what = "data_start")
kardl_extract(kardl_model, what = "data_end")
kardl_extract(kardl_model, what = "data_frequency")
kardl_extract(kardl_model, what = "data_deltat")
kardl_extract(kardl_model, what = "data_tsp")
head(kardl_extract(kardl_model, what = "data_time"))
kardl_extract(kardl_model, what = "no_constant")
kardl_extract(kardl_model, what = "trend")
kardl_extract(kardl_model, what = "asym_long_vars")
kardl_extract(kardl_model, what = "asym_short_vars")
kardl_extract(kardl_model, what = "deterministic")
kardl_extract(kardl_model, what = "dependent_var")
kardl_extract(kardl_model, what = "independent_vars")
kardl_extract(kardl_model, what = "all_vars")
kardl_extract(kardl_model, what = "all_asym_vars")
kardl_extract(kardl_model, what = "indep_as_excluded")
kardl_extract(kardl_model, what = "indep_al_excluded")
kardl_extract(kardl_model, what = "short_run_vars")
kardl_extract(kardl_model, what = "long_run_vars")
kardl_extract(kardl_model, what = "shortrun_length")
kardl_extract(kardl_model, what = "lag_rows_number")
kardl_extract(kardl_model, what = "model_type")
# kardl_extract(kardl_model, what = "data")
kardl_extract(kardl_model, what = "start_time")
kardl_extract(kardl_model, what = "end_time")
kardl_extract(kardl_model, what = "span")
kardl_extract(kardl_model, what = "opt_lag")
kardl_extract(kardl_model, what = "lag_criteria")
kardl_extract(kardl_model, what = "all_cr_lags")
kardl_extract(kardl_model, what = "model_formula")
kardl_extract(kardl_model, what = "k")
kardl_extract(kardl_model, what = "n")




# Examples of extracting components from a kardl_mplier object
\donttest{
# This long-running example won't be tested by CRAN
m <- mplier(kardl_model, horizon = 40)
head(kardl_extract(m, what = "multipliers"))
kardl_extract(m, what = "omega")
head(kardl_extract(m, what = "lambda"))
kardl_extract(m, what = "horizon")

# Examples of extracting components from a kardl_boot object
boot_results <- bootstrap(kardl_model, horizon = 40, replications = 2)
head(kardl_extract(boot_results, what = "multipliers"))
kardl_extract(boot_results, what = "level")
kardl_extract(boot_results, what = "replications")
kardl_extract(boot_results, what = "horizon")
}
# Examples of extracting components from a kardl_test object
test_results <- psst(kardl_model)
kardl_extract(test_results, what = "type")
kardl_extract(test_results, what = "case")
kardl_extract(test_results, what = "statistic")
kardl_extract(test_results, what = "method")
kardl_extract(test_results, what = "alternative")
kardl_extract(test_results, what = "data.name")
kardl_extract(test_results, what = "sample.size")
kardl_extract(test_results, what = "hypotheses")
kardl_extract(test_results, what = "var_names")
kardl_extract(test_results, what = "k")
kardl_extract(test_results, what = "n")
kardl_extract(test_results, what = "sig")
kardl_extract(test_results, what = "notes")

# Examples of extracting components from a kardl_test_summary object
test_summary <- summary(test_results)
kardl_extract(test_summary, what = "statistic")
kardl_extract(test_summary, what = "case")
kardl_extract(test_summary, what = "variables")
kardl_extract(test_summary, what = "decision")
kardl_extract(test_summary, what = "hypotheses")
kardl_extract(test_summary, what = "numeric_decision")
kardl_extract(test_summary, what = "significance_level")
kardl_extract(test_summary, what = "critical_values")
kardl_extract(test_summary, what = "k")
kardl_extract(test_summary, what = "notes")

# Examples of extracting components from a kardl_symmetric object
symmetry_results <- symmetrytest(kardl_model)
kardl_extract(symmetry_results, what = "long_wald_summary")
kardl_extract(symmetry_results, what = "long_hypotheses")
kardl_extract(symmetry_results, what = "short_wald_summary")
kardl_extract(symmetry_results, what = "short_hypotheses")
kardl_extract(symmetry_results, what = "long_wald_tests")
kardl_extract(symmetry_results, what = "short_wald_tests")
kardl_extract(symmetry_results, what = "vars")
kardl_extract(symmetry_results, what = "type")
kardl_extract(symmetry_results, what = "call")

# Example of extracting specific components from symmetry test results
kardl_extract(symmetry_results, what = "short_wald_tests", variable = "PPI")
kardl_extract(symmetry_results, what = "long_wald_tests", variable = "PPI")
kardl_extract(symmetry_results, what = "long_hypotheses", variable = "PPI")
kardl_extract(symmetry_results, what = "short_hypotheses", variable = "PPI")
kardl_extract(symmetry_results, what = "short_hypotheses", component = "H0")
kardl_extract(symmetry_results, what = "short_hypotheses", component = "H1")

kardl_extract(symmetry_results,
  what = "short_hypotheses",
  variable = "PPI", component = "H0"
)

}
